# -----------------------------------------------------------------------------
#
# Gemfile used for CI testing.
#
# -----------------------------------------------------------------------------
source "http://rubygems.org"

group(:test) do
  gem('rake', '>= 0.9.2')
  gem('test-unit', '>= 3.1.8')
  gem('ffi-geos', '>= 0.0.4')
  gem('rgeo', '>= 0.3.13')
  gem('rdoc', '>= 3.12')
end
